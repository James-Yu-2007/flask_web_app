name: Super-Linter

on: push

jobs:
  super-lint:
    name: Lint code base
    runs-on: ubuntu-latest
    steps:

      - name: Run Super-Linter
        run: |
          echo $SECRET_TEST
        env:
          SECRET_TEST: ${{ secrets.SECRET_TEST }}
